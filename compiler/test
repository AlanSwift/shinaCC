int factorial(int n);

void g();

int main()
{
    int a, b;
    int (*fp)(int);
    fp = factorial;
    a = 10;
    (a, b, g());
    fp(a);
    return 0;
}


int factorial(int n)
{
    if(n <= 1)
        return 1;
    return n * factorial(n - 1);
}

